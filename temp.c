#include <stdio.h>
#include <math.h>

void test(char *c){

	//sprintf(c, "Value of Pi = ");
	//puts(c);

}

int main () {
   char str[80],c[20]="1";
   int i,j=0;
   //test(str);
  //puts(c);
  j=c[0]-'0';
   printf("%c          ,%d",c[0],j);


  // puts(str);
 //  for(i=0;str[i]!=NULL;i++)
 //  printf("%c",str[i]);
   
   return(0);
}
